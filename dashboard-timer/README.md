# Dashboard Timer

ESPHome Konfigurationen für das Dashboard Timer Video.

## Dateien

- `home-assistant-voice-pe.yaml` – HA Voice PE Konfiguration (Timer-Sensor-Export)
- `waveshare-dashboard.yaml` – Waveshare 7" LVGL Dashboard Konfiguration

